# Discord Bot（必須）
DISCORD_TOKEN=your_bot_token_here

# 開発モード時のみ使用。開発用サーバーのギルド ID（DEV_MODE=1 または --dev のとき、そのサーバーにだけ Slash コマンドを即時反映）
# DEV_GUILD_ID=your_dev_guild_id_here

# 開発モード: 1 / true / yes で有効。有効時は DEV_GUILD_ID にだけコマンド同期。未設定時は全ギルドにグローバル同期。
# DEV_MODE=1
